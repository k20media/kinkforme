version: '3.6'

services:
  nextjs:
    container_name: kinkforme-nextjs
    image: node:16
    volumes:
      - ../nextjs-app:/var/www/html/nextjs-app
    working_dir: /var/www/html/nextjs-app
    environment:
      - NODE_ENV=development
    expose:
      - "3000"
    ports:
      - "3000:3000"
    command: ["npm", "run", "dev"]
